// #include "pwm_rpm.h"
// #include "myconfig.h"

// void calculateFanRPM()
// {
//     lastFanRPM = (fanPulseCount / 2) * (60 / config.interval / 1000);
//     Serial.print("Fan RPM: ");
//     Serial.println(lastFanRPM);
//     fanPulseCount = 0;
// }

// void calculatePumpRPM()
// {
//     lastPumpRPM = (pumpPulseCount / 2) * (60 / config.interval / 1000);
//     Serial.print("Pump RPM: ");
//     Serial.println(lastPumpRPM);
//     pumpPulseCount = 0;
// }